# Python
backend/.venv/
__pycache__/
*.pyc

# Secrets & keys — never commit
backend/.env
backend/keys/
backend/data/
deploy/keys/
deploy/certs/
*.db

# Caddy runtime
deploy/caddy_data/
deploy/caddy_config/

# Handoff / scratch planning docs — working notes only, never committed
HANDOFF.md

# Basic-auth credentials guarding the site (bcrypt hash)
deploy/.env.auth
backend/.env.*

# TTS cache written by a backend run from the repo root (see app/tts.py).
/data/
